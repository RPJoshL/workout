<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Get the current network state and allow internet access -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <!-- Allow vibrating the device -->
    <uses-permission android:name="android.permission.VIBRATE" />

    <application
        android:allowBackup="true"
        android:name=".RPout"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.RPout"
        android:usesCleartextTraffic="true"
        tools:targetApi="31">

        <meta-data
            android:name="com.google.android.gms.version"
            android:value="@integer/google_play_services_version" />

        <!-- Main activity -->
        <activity
            android:name=".activities.main.MainActivity"
            android:exported="true"
            android:theme="@style/Theme.RPout">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <!-- Login activity -->
        <activity
            android:name="de.rpjosh.rpout.android.activities.login.LoginActivity"
            android:exported="false" android:theme="@style/Theme.RPout"
            android:windowSoftInputMode="adjustResize"
        />
        <!-- Settings activity -->
        <activity
            android:name="de.rpjosh.rpout.android.activities.settings.SettingsActivity"
            android:exported="false" android:theme="@style/Theme.RPout"
            android:windowSoftInputMode="adjustResize"
        />

        <!-- "Download" log file -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="de.rpjosh.rpout.android.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

        <!-- Receive messages from wear os -->
        <meta-data
            android:name="com.google.android.wearable.capability"
            android:value="android" />
        <service android:name="de.rpjosh.rpout.android.services.DataSyncListener"
            android:exported="true">
            <intent-filter>
                <action android:name="com.google.android.gms.wearable.MESSAGE_RECEIVED" />
                <action android:name="com.google.android.gms.wearable.CHANNEL_EVENT" />

                <!-- @TODO listen only for specific events with android:path="/myPath" -->
                <data android:scheme="wear" android:host="*"/>
            </intent-filter>
        </service>


    </application>

</manifest>